{
  "name": "DeveloperEnv",
  "image": "ghcr.io/naik-aakash/autoplex:3.10",
  "hostRequirements": {
   "cpus": 4,
   "memory": "16gb",
   "storage": "32gb"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker"]
    },
  },
  "postCreateCommand": "pip cache purge && pip install -e .[strict,docs] && pip install ase==3.23.0 && pre-commit install",
}
